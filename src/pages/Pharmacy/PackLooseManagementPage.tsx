import { MainLayout } from '@/components/Layout/MainLayout';
import PackLooseManagement from '@/components/Pharmacy/PackLooseManagement';

export default function PackLooseManagementPage() {
  return (
    <MainLayout>
      <PackLooseManagement />
    </MainLayout>
  );
}
